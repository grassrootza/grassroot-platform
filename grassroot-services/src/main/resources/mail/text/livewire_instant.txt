GRASSROOT LIVEWIRE FLASH ALERT

[[ #{livewire.instant.preamble(${contactName}, ${contactNumber})} ]]

====================

[[ ${headline} ]]

====================

[[ #{livewire.instant.groupdetails(${numberMembers}, ${lengthTime}, ${numberTasks})} ]]

<!-- [[ #{livewire.instant.location(${groupCoord},${groupAddress},${alertCoord},${alertAddress})} ]] -->

Further details:

[[ ${description} ]]

You are receiving this mail as part of Grassroot's LiveWire service. LiveWire connects community organizers
to the media by enabling users of the Grassroot app to generate media alerts. For more information please
go to [[ ${infoLink} ]], or [[ ${unsubLink} ]] to unsubscribe.